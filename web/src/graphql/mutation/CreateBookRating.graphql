mutation CreateBookRating($input: BookRatingInput!) {
  createBookRating(bookRatingInput: $input) {
    errors {
      field
      message
    }
    item {
      id
      volumeId
      title
      rating
      status
      creatorId
    }
  }
}
